# Web framework
Flask==3.0.3
Flask-Cors==5.0.0
flask-smorest==0.44.0
Werkzeug==3.0.3

# Environment & config
python-dotenv==1.0.1

# Database / Auth
supabase==2.12.0
PyJWT==2.9.0

# Data processing
pandas==2.2.2
pillow==10.3.0

# Rate limiting
Flask-Limiter==3.5.0

# AI — Google Gemini 2.0 Flash
# Note: we use the openai package as an HTTP client against Google's
# OpenAI-compatible endpoint (generativelanguage.googleapis.com/v1beta/openai/).
# The actual model is Gemini, not OpenAI — see src/utils/models.py.
openai==1.55.3
httpx==0.27.2

# Production server
gunicorn==21.2.0
